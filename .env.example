# Never commit real API keys. Put one or more keys here locally or in Render env vars.
# Separate keys with commas. Argus rotates keys and keeps local RPM/RPD counters per key.
GEMINI_API_KEYS=AIza_example_key_1,AIza_example_key_2
GEMINI_MODEL=gemini-2.5-flash
GEMINI_RPM_LIMIT=10
GEMINI_RPD_LIMIT=250
GEMINI_TIMEOUT_SECONDS=90

# Protects /api/* endpoints that can spend quota or expose reports.
# Set the same secret in Render and enter it in the web UI.
ADMIN_TOKEN=change-me-long-random-secret
AUTH_REQUIRED=true

# Persistent history. DATABASE_URL wins when set; DATABASE_PATH is only local SQLite fallback.
# Supabase/Render Postgres example. Supabase SSL is enforced automatically,
# but keeping sslmode=require in deploy env is still recommended.
# DATABASE_URL=postgresql://postgres:password@host:5432/postgres?sslmode=require
DATABASE_URL=
DATABASE_PATH=data/argus.db
REQUIRE_POSTGRES=false
EVIDENCE_DIR=evidence
EXPORT_DIR=exports

# Optional Kazakhstan proxy. Without it Argus runs in direct fallback mode and
# marks evidence as server-network checks. Set REQUIRE_KZ_PROXY=true only when
# launches must be blocked until a real Kazakhstan proxy is configured.
# Supported names: KZ_PROXY_URL, KZ_HTTP_PROXY, KZ_HTTPS_PROXY, KZ_PROXY.
REQUIRE_KZ_PROXY=false
KZ_PROXY_URL=
KZ_PROXY_CHECK_URL=https://api.country.is/
KZ_ACCESS_LABEL=

MAX_CANDIDATES_PER_RUN=50
MAX_MIRROR_CHECKS_PER_RUN=6
REQUEST_TIMEOUT_SECONDS=18
SCREENSHOTS_ENABLED=true
OSINT_FEEDS_ENABLED=true
OSINT_CANDIDATE_POOL_SIZE=350
ML_ENABLED=true
ML_MODEL_PATH=models/domain_classifier.cbm
CYBERSCAN_MODEL_PATH=models/cyberscan_model.pkl
ML_MIN_CONFIDENCE=0.45

# Optional extra focus. Leave empty for automatic Argus search.
# Example: casino mirror login;betting mirror no license;investment scam reviews
SEED_QUERIES=
